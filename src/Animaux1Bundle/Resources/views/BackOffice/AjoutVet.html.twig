{% extends 'BackOffice/home.html.twig' %}

{% block listeSuivi %}
<div class="grid-form1">
    <h3>Ajout veterinaire </h3>
    <div class="tab-content">
        <div class="tab-pane active" id="horizontal-form">
            {{ form_start(f) }}

            {{  form_end(f)  }}
        </div>
        {{ form_widget(f.ajout) }}
    </div>
    </form>
</div>
</div>

{% endblock %}